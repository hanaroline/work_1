# 탐침 — 금투협에서 과거 설정원본을 받을 수 있는가

탐침 시각: 2026-08-29T10:49:59.741Z

**판정: 판정 보류 — 본문에 날짜꼴이 없다. 기준일자를 어디에 넣는지 본문만으로는 알 수 없다. 잡아 둔 본문 원본을 사람이 읽어야 한다.**

자금유입은 유입액 필드가 아니라 **설정원본의 차이**로 낸다. 설정원본은
좌수 × 1,000 이라 수익률로 움직이지 않는다. 3개월 전 설정원본만 있으면 된다.

태그는 이름으로 찾지 않고 **우리가 아는 오늘 값과 맞는지로** 찾았다.

## 과정

- 표본 8개 (설정액 1.22e+13 ~ 2.41e+8)
- 오늘 기준일 2026-08-27
- 팝업을 열어 오가는 POST 를 잡는다…
- POST 8건을 잡았다
- 설정원본으로 보이는 태그: uOriginalAmt(백만원), netAsstotAmt(백만원)
- 순자산으로 보이는 태그: netAsstotAmt(백만원)
- 본문 안의 날짜꼴 토큰: (없음)
- 재생 확인 — KR5105409225 설정원본 12032812000000 (우리 값 12245199995973)
- 판정 보류 — 본문에 날짜꼴이 없다. 기준일자를 어디에 넣는지 본문만으로는 알 수 없다. 잡아 둔 본문 원본을 사람이 읽어야 한다.

원본 응답과 본문은 `fund_kofia_flow.json` 에 있다.
