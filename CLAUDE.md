# 이 저장소에서 일하는 규칙

내용을 어떻게 쓰는지는 `docs/briefing-playbook.md` 에 있습니다(1600여 줄).
이 파일은 그것과 다릅니다 — **무엇을 통과해야 「다 됐다」고 말할 수 있는지**만
적습니다. 지침은 읽고 잊히지만 끝 상태는 잊히지 않습니다.

## 1. 관문을 통과하지 않은 것은 완성이 아닙니다

브리핑·리포트 HTML 을 만들거나 고쳤으면, 사용자에게 건네기 전에:

```bash
bash scripts/gate.sh docs/briefings/<파일>.html     # 판 하나
bash scripts/gate.sh --changed                      # 고친 판 모두
```

**끝 상태가 0 이 아니면 「완료」라고 말하지 마십시오.** 답에는 관문 요약을
그대로 붙이십시오 — 「검증했습니다」라는 말은 근거가 아닙니다. 끝 상태와
`out/gate/` 의 기록이 근거입니다.

막혔으면 고치고 다시 돌립니다. 검사를 끄거나, 한도를 늘리거나, 관문을 건너뛰는
방식으로 통과시키지 마십시오. 한도를 바꿔야 한다고 판단하면 그 근거를 재서
사용자에게 먼저 말하십시오.

## 2. 왜 이것이 있는가 — 재어 본 사실

검사 스크립트는 예전에도 여덟 개가 있었습니다. 그런데 재어 보니 이랬습니다.

| 스크립트 | 찍은 것 | 끝 상태 |
|---|---|---|
| `audit_numbers.py` | 「어긋난 짝 63개」 | **0** |
| `dupcheck.py` | 「중복 3종」 | **0** |
| `stale.py` | 「합계 5」 | **0** |
| `build_archive_nav.py --check` | 「고칠 곳 25개」 | **0** |

세어 놓고 아무도 막지 않으면 그것은 검사가 아니라 소감입니다. 그래서
「검증했습니다」가 실제로는 「돌려 보고 제가 읽었습니다」였고, 믿을 근거도
남지 않았습니다. 결과가 어떻게 나타났는지도 재어 두었습니다 — 모닝 판의
어제 문장 반복률이 8월 초 4~18% 에서 마지막 주 34~48% 로 올라앉았습니다.
지침 7-2-1 이 경고한 그대로 됐습니다. 경고만 있었고 관문이 없었기 때문입니다.

## 3. 관문은 판단이 필요 없는 것만 막습니다

| 막습니다 | 막지 않고 보여줍니다 |
|---|---|
| 못(수집본 지문)이 박혀 있나 | `stale.py` ①②③ (물려 짓기·시간말·방향) |
| 오늘 판이면 시세가 오늘 것인가 | 0.05%p 미만 반올림 차이 |
| `MARKET DATA` 칩이 자료와 맞나 | 자료칩이 아닌 값 |
| 구조·파생계산 (`recheck.py`) | `recheck` 의 WARN |
| 판 안에 그대로 겹치는 **글월** | 표 머리행 중복(정상입니다) |
| 어제와 같은 문장이 25% 이상 | |
| 브리핑 목록 정합성 | |
| 검증 노트가 있나 | |

**정밀하지 않은 검사를 관문으로 쓰지 마십시오.** `stale.py` ③ 으로 막아 보려
했는데 27 개 판에서 67 건이 잡히고 표본 9 건 중 8 건이 헛경보였습니다. 헛경보가
섞인 빨간불은 사람을 「어차피 오탐」으로 길들입니다 — 애초에 검사가 무력해진
경로가 바로 그것입니다.

## 4. 검사기를 새로 만들거나 고칠 때

1. **끝 상태를 주십시오.** 찍기만 하는 검사는 관문에 넣을 수 없습니다.
2. **헛경보율을 재십시오.** `docs/briefings/*.html` 27 개에 돌려 몇 건이
   잡히는지 세고, 표본을 손으로 확인하십시오.
3. 헛경보가 남으면 `scripts/gate.sh` 의 **[확인]** 칸에 두십시오. 차단은
   판단할 것이 없는 것만입니다.
4. 한도(예: 반복률 25%)는 **재서** 고르고, 그 측정값을 코드 주석에 남기십시오.

## 5. 수치 자료는 근거를 못박고 시작합니다

- 숫자·기관 전망이 들어가는 산출물은 `fin-data-integrity` 스킬을 쓰십시오
  (사용자가 「검증」을 말하지 않아도).
- 브랜드 산출물의 색·폰트·표 규격은 `mas-design` 스킬을 쓰십시오.
- 발행할 판에는 **못을 박으십시오**:
  `python3 scripts/gate/pin.py --auto docs/briefings/<파일>.html`
  못은 그 판이 어느 수집본으로 쓰였는지를 지문과 함께 판 안에 적습니다.

## 6. 수집본을 지우지 마십시오

`data/market/runs/` 에는 수집마다 파일이 하나씩 쌓입니다. 이것이 나중에 판을
다시 검산할 수 있는 유일한 근거입니다.

예전에는 하루 네 번(06:20·06:45·15:40·16:00 KST) 수집하면서 보관은
`data/market/<날짜>.json` **한 파일**이라 뒤엣것이 앞엣것을 덮었습니다. 그래서
오후 수집만 남고 **모닝 판이 실제로 읽은 새벽 수집본은 사라졌습니다.** 밤사이
미국·유럽 마감은 새벽본에만 있으므로, 오후가 지나면 그날 모닝 판은 아무도
검산할 수 없었습니다. 같은 판을 남은 파일들로 대조하면 어긋난 짝이
63 / 87 / 104 로 갈렸습니다 — 판이 변한 것이 아니라 근거가 지워진 것입니다.

`data/market/<날짜>.json` 과 `latest.json` 은 종전 스크립트를 위해 그대로
둡니다. **검산의 근거는 `runs/` 입니다.**

## 7. 답할 때

- 통과하면: 관문 요약(무엇이 통과했고 [확인] 칸에 무엇이 남았는지)을 붙입니다.
- 막혔으면: 무엇이 막았는지와 고친 내용을 말합니다. 숨기지 마십시오.
- 확인하지 못한 것은 **단정문으로 쓰지 마십시오.** 판에는 `1 SOURCE` ·
  `NOT FOUND` 칩이 있습니다. 모르는 것은 그렇게 표시하고, 그 문장은
  「…입니다」로 끝내지 마십시오.
