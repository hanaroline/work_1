# CLAUDE.md — 작업 수칙

이 파일은 이 저장소에서 작업하는 모든 Claude 세션(Claude Code / Chat / Cowork)이
따라야 하는 규칙입니다. **사내 「스마트한 AI 활용을 위한 가이드라인」을 이 프로젝트
상황에 맞게 구체화한 것**이며, 사내 가이드라인이 항상 우선합니다.

---

## 0. 가장 먼저 알아야 할 것

- **이 저장소는 GitHub `public`(전체 공개)이며 GitHub Pages로 배포됩니다.**
  커밋하는 모든 것은 인터넷 전체에 공개된다고 전제하고 작업할 것.
- 따라서 "AI에 입력하지 않는다"로는 부족하고, **"커밋하지 않는다"**가 실질적인 방어선입니다.

---

## 1. 중요정보·개인정보 노출 (가이드라인 ①)

**금지 — 절대 커밋하지 않는다**

- 고객 실명, 계좌번호, 연락처, 생년월일, 거래내역 등 개인정보
- 사내 미공개 정보: 회의록, 내부 보고서, 정책·전략·실적 자료
- API 키, 토큰, 비밀번호 (`.kis-token.json`, `.env` 는 `.gitignore`에 등재되어 있음 — 유지할 것)

**현재 상태 (유지할 것)**

- `고객제안서_*.xlsx`, `*.xlsm` 은 **git 미추적 상태**입니다. 추적으로 바꾸지 말 것.
- 새로 만드는 고객 대상 산출물도 동일하게 미추적으로 둘 것.

**작업 규칙**

- 고객 데이터를 다뤄야 하면 **가명화·마스킹된 샘플**로 대체해서 작업한다.
  (예: `홍길동` → `고객A`, 계좌번호 → `***-**-****`)
- 내부 문서를 요약·재편집하기 전에, 개인정보·민감정보·중요정보 포함 여부를 먼저 확인한다.
- 커밋 전 `git diff --staged` 로 무엇이 올라가는지 확인하고, 위 항목이 보이면 **중단하고 사용자에게 보고**한다.
- 새 파일을 만들 때 고객·실적·전략 관련이면 먼저 `.gitignore` 등재 여부를 묻는다.

---

## 2. 금융소비자보호법 저촉 (가이드라인 ②)

이 저장소는 **고객 제안서·ETF 제안·ELS 상품 검색·목표주가** 등 고객 응대에
직결되는 산출물을 만듭니다. 이 항목이 가장 위험도가 높습니다.

- **AI가 생성·요약한 내용을 고객 제공용으로 그대로 내보내지 않는다.** 항상 내부 초안이다.
- 모든 수치에 **출처와 기준시각**을 함께 표기한다. "최신" 같은 표현 금지.
  (예: `KOSPI 2,650.12 (2026-09-22 종가, 네이버 금융)`)
- 증권사 리포트 다이제스트(`docs/reports/`)는 **내부 참고용**이다. 고객 응대 근거로 쓰지 않는다.
- 수익률·목표주가·상승여력을 **임의로 계산하거나 추정하지 않는다.** 원문에 적힌 값만 옮긴다.
  (`scripts/fetch_reports.py` 의 기존 원칙과 동일 — 셈은 하지 않는다)
- 투자 권유로 읽힐 수 있는 단정적 표현을 쓰지 않는다:
  ~~"오를 것", "추천", "지금이 기회"~~ → "리포트에 따르면 …로 전망", "A증권 목표주가 X원 제시"
- 숫자가 들어가는 자료(PPT/Word/HTML/Excel)를 만들 때는 **`fin-data-integrity` 스킬을 반드시 사용**한다.
  사용자가 "검증"을 언급하지 않아도 적용한다.

---

## 3. 지적재산권 (가이드라인 ③)

- 타사(증권사·언론사) 리포트 **본문을 재배포하지 않는다.** 원문 링크로 연결한다.
- 인용은 출처(증권사명·애널리스트·발행일)를 반드시 병기하고, 필요한 최소 범위로 한정한다.
- `data/reports/*.json` 은 리포트 원문 문장 추출본을 포함하고 **공개 Pages로 배포**된다.
  이 영역을 확대하는 변경(추출 문장 수 증가, 전문 저장 등)은 **임의로 하지 말고 사용자에게 먼저 확인**한다.
- 외부 라이브러리·데이터·이미지를 새로 들여올 때 라이선스를 확인하고 출처를 남긴다.
- 사내에서 작성한 자료를 외부 AI 서비스에 업로드하는 것 자체가 IP 리스크임을 인지한다.

---

## 4. 회사 평판 (가이드라인 ④)

- 특정 기업·업종·국가·집단에 대한 폄훼, 차별적·혐오적 표현을 생성하지 않는다.
- 종목 코멘트는 사실 기반으로만 쓴다. 감정적·선정적 수사를 쓰지 않는다.
- 미래에셋 브랜드 산출물(HTML·리포트·PPT·Word)은 **`mas-design` 스킬**을 적용해
  색·폰트·레이아웃을 일관되게 유지한다.
- 외부에 나갈 수 있는 산출물은 문구를 사전 검토한 뒤 사용자에게 확인받는다.

---

## 5. 거짓 답변(환각) (가이드라인 ⑤)

- **AI는 보조수단이다.** 최종 판단과 검수는 사람이 한다.
- 시세·환율·금리·목표주가·시가총액을 **기억에 의존해 쓰지 않는다.** 반드시 데이터 소스에서 읽는다.
- 확인하지 못한 값은 추측해서 채우지 않는다. `확인필요` 로 남기고 사용자에게 보고한다.
- 2차 보도(기사)를 1차 출처(기관 발표)로 표기하지 않는다.
- 코드를 고친 뒤 "완료"라고 보고하기 전에 실제로 실행·검증한다. 검증하지 못했으면 그렇게 말한다.

---

## 6. 실행 파일·스크립트 작업 시

- `scripts/`, `tools/`, `.github/workflows/` 변경은 **자동 수집 파이프라인에 직접 영향**을 준다.
  변경 전 해당 워크플로가 무엇을 언제 돌리는지 확인하고, 변경 내용을 사용자에게 설명한다.
- 외부 API를 새로 호출하는 코드를 추가할 때:
  - 인증정보는 **반드시 GitHub Secrets / 환경변수**로. 코드에 하드코딩 금지.
  - 호출 빈도·이용약관(크롤링 허용 범위)을 확인한다.
- 생성물을 저장소에 커밋하는 스크립트를 만들 때는, 그 생성물이 공개돼도 되는지 먼저 확인한다.
- 사용자 환경에서 실행될 파일(`.xlsm` 매크로 등)을 만들 때는 동작 내용을 명시적으로 설명한다.

---

## 7. 프로젝트 구조 (참고)

- 단일 HTML 대시보드. 빌드 도구 없음.
- `index.html` + `data/*.js` + `vendor/*.js` → 합쳐서 `standalone.html` 생성
- `docs/reports/index.html` — 증권사 리포트 다이제스트 (GitHub Actions가 데이터 갱신)
- 대용량 파일 주의: `README.md`(250KB), `proposal.html`(730KB), `els-product-search.html`(390KB)
  → 통째로 읽지 말고 필요한 범위만 검색해서 볼 것.

## 8. 작업 방식

- 작업 브랜치에서만 작업한다. `main` 직접 푸시 금지.
- PR은 사용자가 명시적으로 요청할 때만 만든다.
- 웹 세션 컨테이너는 휘발성이다. 남겨야 할 결과물은 커밋하거나 사용자에게 파일로 전달한다.
  (단, 1항의 금지 항목은 커밋하지 않는다 — 이 경우 파일 전달만 한다)
