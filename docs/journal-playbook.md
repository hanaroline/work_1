# 국내 시장일지 — 만드는 법

미래에셋증권 마포WM의 **국내 시장일지**를 만드는 규칙입니다. 시황 브리핑
(`docs/briefing-playbook.md`)과는 다른 자료입니다 — 브리핑이 「오늘 무슨
일이 있었나」를 읽는 글이라면, 시장일지는 **「수급과 거래량으로 그날의
주요종목을 짚는」 표**입니다.

이 문서를 고치면 다음 실행부터 반영됩니다.

---

## 0. 무엇을 언제 내는가

| 언제 | 무엇 | 어디에 |
|---|---|---|
| 국내 거래일 15:40 KST | 종목 순위 수집만 | `data/journal/<날짜>.json` |
| 16:00 · 16:20 · 16:45 · 17:15 · 18:00 · 19:20 · 21:00 KST | **자료가 다 찬 첫 판**이 발행 | `docs/journal/<날짜>.html` · `.txt` · 텔레그램 |

**발행을 정하는 것은 시각이 아니라 자료입니다.** 예약을 다섯 번 거는
까닭이 이것입니다 — 마감 시각이 지났다고 자료가 다 찬 것이 아닙니다.
`scripts/check_journal_ready.py` 가 다섯을 보고, 하나라도 어긋나면 내지
않고 다음 판을 기다립니다. 먼저 낸 판이 있으면 뒤의 판은 스스로 물러섭니다.

  1. 정규장이 끝났는가
  2. 순위 파일의 기준일이 오늘인가
  3. 외국인·기관 **둘 다** 오늘 확정치인가
  4. 시세 파일의 종가일도 오늘인가
  5. 오늘 판이 이미 있지는 않은가

### 2026-09-21 에 배운 것 — 16:00 은 이르다

첫 발행을 16:06 에 걸었다가 두 가지에 걸렸습니다. 둘 다 그대로 냈으면
**틀린 판이 나갔을** 자리입니다.

- **네이버는 시간외단일가(16:00~18:00)까지 `marketStatus: OPEN`** 으로
  줍니다. 「장이 닫혔는가」를 그 칸으로 가리면 영원히 닫히지 않습니다.
  갈라 주는 것은 `tradingSessionType` 입니다 — 정규장이면 `REGULAR_MARKET`,
  끝나면 `AFTER_MARKET`.
- **종목별 수급은 주체마다 따로 찹니다.** 16:07 에 외국인은 당일(잠정)
  이었는데 **기관은 아직 전 거래일** 것이었습니다. 그대로 냈으면 한 표에
  오늘 외국인과 어제 기관이 나란히 실렸을 것입니다.
### 기관 종목별은 **밤에** 찹니다 — 2026-09-21 에 재어 보았습니다

하루 동안 여섯 번 재어 시각을 좁혔습니다.

| 잰 시각 (KST) | 외국인 | 기관 |
|---|---|---|
| 11:51 · 16:07 · 16:52 · 18:07 · 19:26 | 20260921 (**잠정**) | **20260918** (확정) |
| 23:45 | 20260921 (확정) | **20260921** (확정) |

읽어 낸 것은 둘입니다.

1. **두 값이 서로 다른 주소에서 옵니다.** 시장 전체 합계는 `trend/daily`
   에서 오고 **처음부터 당일 것**이었습니다. 종목별 내역만
   `trend/trendForeignOrg` 에서 오고 늦게 찹니다. 그래서 「기관이 오늘
   1.5조를 샀다」는 알 수 있어도 「무엇을 샀나」는 한참 모릅니다.
2. **원천은 외국인에게만 장중 잠정치를 줍니다.** 기관에는 잠정치가 아예
   없어서, 확정치가 풀리기 전까지 「마지막으로 확정된 날」을 계속 보여
   줍니다. 그래서 그 칸이 *어제 날짜*로 보이는 것이지, 자료가 없는 것이
   아닙니다.

> 왜 기관만 그런지는 **추정**입니다(원천이 말해 주지 않습니다) — 외국인
> 매매는 외국인투자등록번호가 붙어 장중 집계가 되지만, 기관을 금융투자·
> 투신·사모·연기금으로 **가르는 일**은 장 종료 뒤 회원사 계좌 분류가
> 끝나야 확정되기 때문으로 보입니다.

잠정치는 금액을 주지 않아 수량 × 종가로 어림하는데, 확정치와 **1위 종목까지
바뀔 만큼** 다릅니다. 2026-09-21 코스피 외국인 1위 삼성전자가 ≈9,977억
(어림) → 11,063억(확정), 코스닥 기관 1위는 한양디지텍(9/18) → 하나마이크론
으로 바뀌었습니다.

이 저장소의 「종목별 수급 수집」(`stock-flows.yml`)이 19:00·20:30·22:00 에
도는 것도 같은 벽을 만난 결과로 보입니다. 그래서 발행 판을 19:20·21:00·
22:00·23:00 까지 늘렸습니다. **16:00 발행은 외국인만 당일이고 기관은 전
거래일인 판이 되기 쉽습니다** — 그 판을 내야 한다면 `--allow-stale-investor`
로 「잠정」 표시를 달고 내고, 뒤에 차면 다시 지어 덮으십시오.

### KIS 도 마찬가지입니다 — **「안 움직인다」가 「확정됐다」는 아닙니다**

> **이 절은 한 번 틀리게 적었다가 고친 것입니다.** 처음에는 「KIS 가 마감 7분
> 뒤에 확정치를 준다」고 적었습니다. 네 시간 동안 값이 한 주도 안 움직이는
> 것을 보고 그렇게 판단했는데, **그 뒤에 바뀌었습니다.** 안정은 확정의 증거가
> 아닙니다.

한국투자증권 오픈API(`FHKST01010900` 주식현재가 투자자)를 2026-09-22 에 여섯
번 재었습니다. 삼성전자 기준입니다.

| 잰 시각 (KST) | 맨위 날짜 | 외국인 | 기관 |
|---|---|---|---|
| 15:37 (마감 7분 뒤) | 20260922 | 747,617 | 267,601 |
| 16:02 | 20260922 | 747,617 | 267,601 |
| 17:32 | 20260922 | 747,617 | 267,601 |
| 19:33 | 20260922 | 747,617 | 267,601 |
| **22:05** | 20260922 | **659,851** | **259,459** |

**19:33 과 22:05 사이에 바뀝니다** — 외국인 **−11.7%**, 기관 **−3.0%**.
앞의 네 판은 네 시간 내리 같은 값이었지만 **잠정치였습니다.**

그리고 바뀐 뒤의 값은 네이버 확정치와 **정확히 일치**합니다.

| 2026-09-22 코스피 기관 삼성전자 | |
|---|---|
| 네이버 확정 (22:02 수집) | **259,459** |
| KIS 확정 (22:05 측정) | **259,459** |

전날도 그랬습니다(외국인 4,080,410 · 기관 4,186,614 양쪽 일치). 두 원천은
같은 거래소 자료를 보고, **확정되기 전까지는 둘 다 못 믿습니다.**

**KIS 가 네이버보다 나은 점**은 하나 있습니다 — 기관 칸에 *전 거래일 날짜*를
남겨 두지 않고 당일 잠정치를 줍니다. 그래서 「오늘 것이 아직 없다」를 날짜로
가릴 수 없습니다. 네이버 쪽이 오히려 정직한 셈입니다.

**결론: 16:00 완전판은 불가능합니다.** 확정치는 저녁(19:33~22:05 사이)에
옵니다. 지금 발행 예약(19:20·21:00·22:00·23:00)이 그 구간을 덮고 있으므로
**고칠 것이 없습니다.** 16:00 에 내야 한다면 아래의 잠정 판으로 냅니다.

> **가집계(`FHPTJ04400000`)는 쓰지 마십시오.** 2026-09-21 에 삼성전자 기관이
> 1,431,000주(가집계) 대 4,186,614주(확정)로 **2.9배** 어긋났습니다. 응답의
> 다섯 줄은 누적 시간대(`bsop_hour_gb` 1→5)이고 마지막 누적조차 그렇습니다.
> 이름이 「가집계」인 것을 믿으십시오.

**그래도 원천을 KIS 로 옮길 값어치는 남아 있습니다** — 발행을 앞당기려는
것이 아니라 **금액을 얻으려는** 것입니다. 네이버 잠정치는 금액을 주지 않아
수량 × 종가로 어림하는데, KIS 확정 계열은 금액을 그대로 줍니다. 다만 옮기려면
걸림돌이 있습니다: 확정 계열은 **종목 하나씩만** 주고, 순위표를 주는 쪽은
가집계라 못 씁니다. 그래서 순위를 직접 만들어야 합니다 — 거래대금 상위
300~400 종목을 추려 종목마다 부르고 우리가 정렬합니다. 모의투자 앱키는 초당
1건이라 300종목에 5.5분, 400종목에 7.5분입니다(실전 앱키면 45초).
**어느 쪽이든 저녁에 불러야 합니다.**

**종목별 사모펀드 순매수는 KIS 에도 없습니다.** 순위표의 칸은 외국인·기관계·
투신·은행·보험·종금·기금·기타법인·기타기관 아홉이고 사모펀드는 없습니다.

**휴장일에는 내지 않습니다.** 정리할 「오늘 장」이 없습니다(브리핑의
아침 판과 다른 점입니다). 수집기는 예약이 돌아도 기준일이 전 거래일이면
같은 파일을 다시 쓰고 끝나며, 발행 관문이 기준일 어긋남을 경고합니다.

산출물은 세 쪽입니다.

```
1쪽  본지     — A4 한 장. 지수·수급·주요종목·테마·이슈
2쪽  대시보드 ① 수급   — 종목별 기관·외국인 매매, 쌍매수·쌍매도, 기관 안쪽, ETF
3쪽  대시보드 ② 순위   — 상승률·거래대금 상위 20, 신고가·상한가·거래량 급증
```

---

## 1. 무엇으로 짓는가 — 파일 셋

세션은 사내 이그레스 정책 때문에 KRX·네이버·야후에 직접 붙지 못합니다
(CONNECT 403). **다시 시도하지 마십시오.** 러너가 받아 커밋한 것을 읽습니다.

| 파일 | 담긴 것 | 누가 채우나 |
|---|---|---|
| `data/market/latest.json` | 지수·환율·금리·예탁금·신용잔고·투자자 합계·업종·기사 | `scripts/fetch_market.py` (「시세 수집」) |
| `data/journal/latest.json` | 종목 순위·종목별 수급·ETF·테마 | `scripts/fetch_journal.py` (「시장일지 수집」) |
| `data/flows/kr100.json` | 대형주 100 종목별 외국인·기관 순매수 **누적** | `scripts/fetch_stock_flows.py` |

셋의 **기준일이 같은지 먼저 봅니다.** 어긋나면 빌더가 머리말에 적습니다.
낡은 줄 모르고 쓰는 것이 못 쓰는 것보다 나쁩니다.

---

## 2. 원천과 그 한계 — 적어 두지 않으면 반드시 잊습니다

2026-09-21 에 브라우저 관찰과 요청 스윕으로 확인한 것입니다
(기록: `data/journal/raw/journal_api.txt` · `journal_xhr.txt` ·
`journal_xhr2.txt` · `journal_api2.txt`).

### 종목 순위 — `stock.naver.com/api/domestic/market/stock/default`

한 줄에 등락률·거래대금·거래량·전일대비 거래량증가율·52주 최고가·상한가
구분이 모두 들어 있어, **상승률·거래대금·거래량 급증·신고가·상한가가
주소 하나로 풀립니다.** 순위는 우리가 매깁니다.

- **`startIdx` 로 넘길 수 없습니다.** 올리면 빈 응답이 옵니다. 한 번에
  받는 양은 `pageSize` 가 정하고, 2000 까지 받았습니다. 그래서 코스피·
  코스닥을 따로 받아 잇습니다(2026-09-21 기준 2,762 종목).
- 그러므로 이것은 **시가총액 상위 표본**이지 거래소 상장 전종목이
  아닙니다. 산출물 각주에 받은 종목 수를 적습니다.
- `orderType` 은 `up` · `down` · `priceTop` · `marketSum` 만 열립니다.
  `marketType=ETF` 는 400 입니다.

### 종목별 기관·외국인 — `…/market/trend/trendForeignOrg`

```
?investorType=FOREIGNER|ORGANIZATION&tradeType=KRX&marketType=KOSPI|KOSDAQ
&startIdx=0&pageSize=20&periodType=DAY
→ sections.buyRankList / sellRankList
```

- `investorType` 은 **둘뿐**입니다. `INSTITUTION` · `PENSION` ·
  `PRIVATE_EQUITY` 따위는 모두 400 입니다.
- **장중에는 `accTradeAmount` 가 0 이고 `estimated` 가 참**입니다. 그때
  순위는 금액이 아니라 **수량** 기준이고, 빌더는 수량 × 종가로 어림한 값에
  「≈」를 달고 표 이름에 「수량 기준」이라 적습니다.
- 매도 쪽은 **값이 음수로** 옵니다. 부호를 그대로 두고 내림차순으로 세우면
  가장 적게 판 종목부터 늘어섭니다(실제로 그렇게 나왔습니다). 크기 순으로
  세웁니다.
- 외국인과 기관의 **기준일이 서로 다를 수 있습니다** — 장중에 외국인은
  오늘, 기관은 전 거래일이었습니다. 줄마다 기준일을 달아 둡니다.
- 「쌍매수」는 **두 명단(각 20)에 함께 든 종목**입니다. 전 종목에서 둘 다
  순매수한 종목을 센 것이 아니며, 표 각주에 그렇게 적습니다.

### ETF — `…/stockSecurity/etfs/v2/domestic`

- `listingType` 은 `tradingValueDesc` · `aumDesc` · `changeRateDescUpAll`
  등만 열립니다. `changeRateDesc` 는 400 입니다.
- **`index` 로 넘길 수 없습니다** — `index=100` 이면 빈 응답에 `hasNext`
  도 거짓입니다. 순위마다 판을 따로 받고, 각 판의 **위 100 줄**이 보는
  전부입니다(상장 1,171 종목 가운데).
- 레버리지·인버스는 **이름으로** 거릅니다(레버리지·인버스·2X·곱버스).
  원천의 `leverage-types`(P1 일반·P2 레버리지2X·N1/N2 인버스)는 목록
  응답에 붙어 오지 않습니다.

### 받지 못하는 것 — 비워 두고 NOT FOUND 를 답니다

| 항목 | 왜 |
|---|---|
| **종목별 사모펀드 순매수** | 거래소에만 있고 거래소는 수집 서버 IP 를 막습니다. **시장 합계**까지만 싣습니다(`기관 안쪽 순매수` 표) |
| 연속 순매수 (전 종목) | 원천이 하루치만 줍니다. 누적본(`kr100.json`)이 있는 **대형주 100 종목**만 셉니다 |
| VKOSPI | 무료 원천이 없습니다 |
| 미수금·반대매매 | 네이버에 없고 금투협은 웹방화벽이 막습니다. 신용잔고를 대용으로 씁니다 |

---

## 3. 문턱과 판정 — 왜 그렇게 골랐는가

| 표 | 규칙 | 까닭 |
|---|---|---|
| 상승률·하락률 상위 | **거래대금 10억원 이상** | 문턱이 없으면 하루 몇백만원 거래되는 우선주와 잠든 종목이 상위 20 을 채웁니다. 그 줄은 「오늘 시장이 본 종목」이 아닙니다 |
| 거래량 급증 | 거래대금 **100억원 이상** 가운데 전일 거래량 대비 증가율 상위 | 같은 까닭. 문턱이 없으면 평소 5천만원 돌던 종목이 1등을 합니다 |
| 52주 신고가 | 종가 ≥ 52주 최고가 **이면서 당일 상승** | 원천의 52주 최고가 칸은 최근 며칠을 반영하지 못할 때가 있어, 오름세 조건이 없으면 이미 뚫고 내려온 종목이 며칠씩 이어서 잡힙니다 |
| 상한가 | 원천의 `upDownGb=1` | 가격제한폭이 호가 단위 때문에 29.81·29.9 로 나와 등락률 문턱으로는 가를 수 없습니다 |

---

## 4. 검산이 빌드보다 먼저입니다

```bash
python3 scripts/verify_journal.py              # 끝 상태 1 이면 짓지 않는다
python3 scripts/verify_journal.py --warn-only  # 장중 확인용
```

잡는 것 — 전부 **추가 조회가 필요 없는** 것들입니다.

- 파일 사이 기준일 어긋남
- 인쇄될 등락률이 종가·전일대비와 맞지 않음
- 거래량 증가율이 거래량·전일거래량과 맞지 않음
- 신고가·상한가 판정이 값과 어긋남
- 순위표가 실제로 그 열로 정렬되어 있지 않음
- **투자자별 순매수 합이 0 에서 멂** — 개인+외국인+기관+기타법인(7000·7100)
  은 0 이어야 합니다. 멀면 장중 집계와 확정치가 섞인 것입니다
- 기관 안쪽 합이 기관계와 다름
- 단위 뒤섞임(억원 자리에 원)

**투자자 구분 코드는 `scripts/fetch_market.py` 와 같은 묶음을 씁니다.**
두 파일이 다른 셈을 하면 같은 판 안에서 코스피 수급이 두 값으로 갈립니다.

```
개인   8000
외국인 9000 + 9001(기타외국인)      ← 9000 만 쓰면 하루 수십억씩 어긋난다
기관계 1000 금융투자 · 2000 보험 · 3000 투신 · 3100 사모
       · 4000 은행 · 5000 기타금융 · 6000 연기금
기타   7000 · 7100                   ← 기관계에 넣지 않는다
```

---

## 5. 판형 — 「A4 한 장」은 약속입니다

```bash
node scripts/check_journal_fit.mjs docs/journal/<날짜>.html
```

쪽마다 안쪽 높이를 재어 A4(297mm)를 넘는지 봅니다. 넘으면 표의 줄 수를
줄이거나(빌더의 `cap`) 표를 `dt dense` 로 돌립니다. 넘친 줄만 다음 쪽으로
흘러가면 아무도 읽지 않는 꼬리가 생깁니다.

색·글꼴은 미래에셋 기준을 그대로 씁니다 — 오렌지 `#F58220`, 블루
`#043B72`, 표 머리 `#FAB072`, 1px 오렌지 섹션 룰, 한/영 토글 우상단.
**본문 크기만 인쇄 밀도에 맞춰 줄였습니다**(웹 기준 19px 대신 표 8.5px) —
A4 한 장에 표 열두 개를 세우는 자료라 웹 호흡을 그대로 쓸 수 없습니다.

### PDF

```bash
node scripts/journal_to_pdf.mjs docs/journal/<날짜>.html
```

텔레그램으로 건네는 것이 이 PDF 입니다. 받는 쪽이 휴대폰이라도 A4 네 쪽
그대로 열리고, 브라우저·글꼴에 기대지 않습니다. 구운 뒤 **PDF 의 쪽 수를
세어 `.sheet` 개수와 맞는지 봅니다** — 어긋나면 어느 장이 넘쳐 꼬리 쪽이
생긴 것입니다.

> **함정 — 인쇄할 때 좁은 화면 규칙이 걸린다.** A4 쪽 상자의 안쪽 너비는
> 192mm = **726px** 입니다. 그래서 `@media (max-width:820px)` 로 두면
> *인쇄할 때도* 휴대폰용 규칙이 걸려, 두 단이 한 단으로 풀리고 표가 11px 로
> 커집니다. 실제로 네 장이 **아홉 쪽**으로 구워졌습니다. `@media screen and
> (max-width:820px)` 로 `screen` 을 반드시 붙이십시오. 화면에서 재는
> `check_journal_fit.mjs` 는 794px 너비로 재기 때문에 이 함정을 못 봅니다 —
> 굽고 나서 쪽 수를 세는 까닭이 이것입니다.

인쇄 여백 `@page{margin:9mm 9mm 7mm}` 는 `.sheet` 의 안쪽 여백과 **같은
값**입니다. 어긋나면 화면에서 「들어감」이던 장이 PDF 에서 2mm 넘칩니다.

---

## 6. 손으로 돌리기

```bash
# 자료가 오늘 것인지 먼저
python3 scripts/check_market_fresh.py close
python3 -c "import json;print(json.load(open('data/journal/latest.json'))['bizdate'])"

# 낡았으면 러너에 요청 (세션은 Actions 쓰기 권한이 없어 API 로 못 부릅니다)
printf '요청: %s\n' "$(TZ=Asia/Seoul date '+%F %T KST')" >> data/journal/REFRESH
git add data/journal/REFRESH && git commit -m "시장일지 수집 요청" && git push

# 짓기
python3 scripts/verify_journal.py && python3 scripts/build_journal.py
node scripts/check_journal_fit.mjs docs/journal/$(date +%F).html
node scripts/journal_to_pdf.mjs   docs/journal/$(date +%F).html
```

---

## 7. 텔레그램

`scripts/send_journal_telegram.py` 가 본문(`.txt`)과 HTML 첨부를 보냅니다.
저장소 비밀값 둘이 있어야 합니다.

```
TELEGRAM_BOT_TOKEN   BotFather 가 준 토큰
TELEGRAM_CHAT_ID     보낼 채널·방의 id (채널은 -100… 로 시작)
```

**없으면 보내지 않고 경고만 남깁니다.** 보내지 못한 것을 보낸 척하지
않습니다. 산출물은 저장소에 그대로 남습니다.

---

## 8. 원천이 또 끊기면

네이버는 개편을 자주 합니다 — 2026-09-10 에 상한가 화면이, 09-18 에
투자자별 일별시세가 끊겼습니다. **주소를 짐작해 찍지 마십시오.** 다섯
바퀴를 헛돈 적이 있습니다. 같은 방법을 씁니다.

```bash
# 화면을 열어 그 화면이 보내는 요청을 그대로 적는다 (러너에서만 돈다)
printf '요청: %s\n' "$(TZ=Asia/Seoul date '+%F %T KST')" >> data/journal/PROBE
git add data/journal/PROBE && git commit -m "원천 관찰 요청" && git push
# → data/journal/raw/ 에 기록과 응답 본문이 남는다
```

`scripts/probe_journal_xhr.mjs`(화면 관찰) ·
`scripts/probe_journal_xhr2.mjs`(화면 안에서 직접 호출) ·
`scripts/probe_journal_api.py` · `scripts/probe_journal_api2.py`(평문 요청
스윕)가 차례로 돕니다. 찾은 자리는 **평문 요청으로도 열리는지 확인한 뒤에**
수집기에 붙입니다 — 브라우저에서만 열리는 자리는 날마다 돌릴 수 없습니다.
