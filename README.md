# 종목 통합 리포트 대시보드

주식 종목명 또는 종목코드를 입력하면 해당 종목의 **시세 · 증권사 투자의견/레포트 · 시황(뉴스) · 재무제표**를
**한 장의 리포트**처럼 보여주는 단일 HTML 대시보드입니다. 빌드 도구 없이 `index.html` 하나로 동작합니다.

여기에 **데이터센터 밸류체인 종목 맵(`datacenter.html`)** 이 함께 들어 있습니다.
맵에서 종목을 클릭하면 상세 정보가 열리고, 국내 종목은 위 대시보드로 바로 연결됩니다. → [자세히](#데이터센터-밸류체인-종목-맵)

## 화면 구성

| 섹션 | 내용 |
|------|------|
| **종목 요약** | 종목명·코드·시장(KOSPI/KOSDAQ), 현재가·등락, 시가총액, PER/PBR/EPS, **조회 기준 시각** |
| **시세 · 기술적지표** | 가격 차트(1M/3M/6M/1Y) + **이동평균 MA5/20/60** + **RSI(14)** + 거래량, 52주 최고/최저, **지수 대비 초과수익률** |
| **외국인 · 기관 수급** | 일별 순매수 대금(외국인/기관) 차트 + 외국인 지분율 추이, 5일/20일 누적 순매수 |
| **증권사 투자의견** | 목표주가 밴드(최저·평균·최고 vs 현재가), 상승여력, 매수/중립/매도 의견분포, 최근 레포트 목록 |
| **시황 · 뉴스** | 종목 관련 최신 뉴스 헤드라인 |
| **재무제표** | 연간 손익(매출·영업이익·순이익) 차트 + 표, 재무상태(자산·부채·자본), ROE·부채비율 |

### 주요 기능
- **관심종목 즐겨찾기(★)** — 브라우저에 저장(localStorage), 상단 칩에서 원클릭 조회
- **종목 비교(⇄)** — 최대 4종목을 기준=100 리베이스 차트 + 지표 표로 비교
- **지수 대비 보기** — 가격/지수대비(KOSPI·KOSDAQ 리베이스) 토글, 초과수익률 표시
- **PDF 저장/출력(🖨)** — 화면 그대로 A4 리포트로 저장·인쇄(`@media print`)
- 라이트/다크 테마 전환(◐), 종목명 자동완성, 6자리 코드 직접 입력

## 실행 방법

### 방법 1) 단일 파일 (가장 간단)

`standalone.html` **한 파일**에 CSS·JS·차트 라이브러리·데이터가 모두 인라인되어 있습니다.
별도 서버 없이 파일을 브라우저로 열기만 하면(더블클릭) 바로 동작합니다.

```
standalone.html  ← 이 파일만 열면 됨
```

### 방법 2) 분리 파일 + 로컬 서버 (개발용)

`index.html`은 `data/*.js`, `vendor/*.js`를 외부에서 로드하므로 로컬 서버로 열어야 합니다
(파일 프로토콜로 열면 브라우저 보안정책에 막힐 수 있음).

```bash
# 저장소 루트에서
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

> `standalone.html`은 `index.html` + `data/*.js` + `vendor/chart.umd.js`를 합쳐 생성한 산출물입니다.
> 소스를 수정할 때는 `index.html` / `data/*.js`를 고친 뒤 다시 합치면 됩니다.

## 데이터 소스

무료 공개 소스를 우선 사용하고, 실패 시 **섹션별로 샘플 데이터로 폴백**합니다.
각 섹션에는 `실시간` / `샘플` 배지가 표시됩니다.

| 섹션 | 실시간 소스 | 폴백 |
|------|-------------|------|
| 시세/현재가/차트 | **네이버 모바일 `m.stock.naver.com/api`** 우선, Yahoo `chart` 보조 | 내장 샘플 |
| 외국인/기관 수급 | 네이버 `api/stock/{code}/trend` | 내장 샘플 |
| 지수(KOSPI/KOSDAQ) | Yahoo `chart/^KS11 · ^KQ11` | 내장 샘플 |
| 재무제표 | Yahoo `quoteSummary` (손익/재무상태) | 내장 샘플 |
| 투자의견/목표주가 | Yahoo `quoteSummary` (financialData, recommendationTrend) | 내장 샘플 |
| 시황/뉴스 | Google News RSS(`news.google.com/rss/search`) | 내장 샘플 |
| 증권사 레포트 목록 | (실시간 API 부재) | 예시 데이터 + 네이버 리서치 딥링크 |

모든 소스는 **병렬 호출 + 4초 타임아웃**으로 요청하며, 실패한 섹션만 개별적으로 샘플로 대체합니다(전체 화면은 항상 정상 렌더). 요약 헤더에 조회 기준 시각과 출처가 표시됩니다.

> 증권사 원문 리포트(PDF)는 공개 API가 없어, 목록은 예시로 채우고
> [네이버 금융 리서치](https://finance.naver.com/research/)로 연결되는 딥링크를 제공합니다.

### 브라우저 CORS 안내

순수 브라우저(단일 HTML)에서 위 API를 직접 호출하면 **CORS로 차단**되는 경우가 많습니다.
이를 위해 `index.html`의 `PROXIES` 배열에 **CORS 프록시 폴백**이 설정되어 있습니다.

```js
var PROXIES = [
  function (u) { return u; },                                              // 1) 직접 호출
  function (u) { return "https://corsproxy.io/?url=" + encodeURIComponent(u); },      // 2) 프록시
  function (u) { return "https://api.allorigins.win/raw?url=" + encodeURIComponent(u); } // 3) 프록시
];
```

- 사내망/폐쇄망 등으로 외부 접근이 막히면 세 경로 모두 실패하고 **샘플 데이터**로 표시됩니다(화면은 항상 정상 렌더).
- 안정적인 실시간 연동이 필요하면, 자체 백엔드 프록시(예: `/api/proxy?url=...`)를 만들어 `PROXIES`에 추가하는 것을 권장합니다.
- 미래에셋 **사내 API**가 있다면 `fetchYahoo` / `fetchNews`를 사내 엔드포인트 호출로 교체하면 됩니다.

---

# 데이터센터 밸류체인 종목 맵

`datacenter.html` — AI 데이터센터 한 곳이 세워질 때 **돈이 흘러가는 순서대로 9개 단계**를 나누고,
각 단계에서 시장 지위를 확보한 **국내·해외 대형 종목 84개**(국내 38 · 해외 46)를 배치한 맵입니다.

### 밸류체인 9단계

| # | 단계 | 핵심 변수 | 국내 예시 | 해외 예시 |
|---|------|-----------|-----------|-----------|
| 01 | AI 반도체 · 연산 | 가속기 출하량, 파운드리 선단 캐파 | 삼성전자 | NVIDIA · Broadcom · TSMC |
| 02 | 메모리 · 스토리지 | HBM 세대 전환, 서버 D램 가격 | SK하이닉스 · 삼성전자 | Micron · Seagate |
| 03 | 기판 · 후공정 · 부품 | 층수·면적 증가, 패키징 캐파 | 한미반도체 · 삼성전기 · 이수페타시스 | Ibiden · Amphenol |
| 04 | 서버 · 시스템 | 랙 출하량, 액체냉각 전환 | 서진시스템 | Dell · Hon Hai · Wiwynn · Delta |
| 05 | 네트워크 · 광통신 | 클러스터 규모, 800G/1.6T 전환 | 오이솔루션 · 대한광통신 | Arista · Cisco · Coherent · Fujikura |
| 06 | 전력 · 에너지 | 계통 접속 대기, 변압기 리드타임 | HD현대일렉트릭 · 효성중공업 · 두산에너빌리티 | Eaton · Vertiv · GE Vernova · Constellation |
| 07 | 냉각 · 공조 | 랙 전력밀도, 액체냉각 채택률 | LG전자 · SK이노베이션 | Vertiv · Johnson Controls · Daikin |
| 08 | 건설 · EPC | 신규 착공, 전기공사 인력 | 삼성물산 · 현대건설 · 삼성E&A | Quanta Services · EMCOR · Comfort Systems |
| 09 | 운영 · 코로케이션 · 클라우드 | 하이퍼스케일러 capex, 임대율 | NAVER · KT · 삼성SDS · KINX | Equinix · Digital Realty · Microsoft · Amazon |

### 기능

- **종목 클릭 → 상세 패널** : 밸류체인 포지션, 한 줄 요약, **데이터센터 연관 포인트**, 주요 제품·서비스,
  경쟁 지위, 해당 단계, **같은 단계 종목(클릭해 이동)**, 외부 링크
- **실시간 시세** : 국내는 네이버 모바일 API, 해외는 Yahoo Finance API로 조회 시점 시세를 표시
  (외부 접근이 막힌 환경에서는 시세 영역만 안내 문구로 대체되고 화면은 정상 동작)
- **대시보드 연동** : 국내 종목은 `index.html?code=<6자리코드>` 딥링크로 통합 리포트 화면을 바로 열 수 있음
  (대시보드 상단의 `밸류체인 맵` 버튼으로 되돌아옴)
- **필터** : 국내/해외, 전체 규모/대형주만, 종목명·티커·키워드 검색
- **한/영 토글** : 우상단 KO/EN, 단계 설명·종목 설명까지 전량 번역, 선택은 localStorage에 저장
- **인쇄/PDF** : `@media print` 적용 — 컨트롤·패널을 숨기고 맵만 A4로 출력

### 실행 방법

**권장 — `serve.py`로 실행 (시세 조회까지 정상 동작)**

```bash
python3 serve.py          # → http://localhost:8000/datacenter.html
python3 serve.py 9000     # 포트 지정
```

정적 파일 서비스 + **시세 API 프록시(`/api/proxy`)** 를 함께 제공합니다.
브라우저가 아니라 서버가 네이버·Yahoo를 호출하므로 CORS 차단이 발생하지 않습니다.
사내 프록시가 필요한 망에서는 환경변수를 그대로 사용합니다.

```bash
HTTPS_PROXY=http://proxy.company.co.kr:8080 python3 serve.py
```

**간단 실행**

```
datacenter-standalone.html   ← 단일 파일. 더블클릭으로 바로 열림 (시세는 막힐 수 있음)
datacenter.html              ← data/dc-chain*.js 를 로드하므로 로컬 서버 필요
```

```bash
python3 -m http.server 8000   # 프록시 없는 단순 서버
```

### 시세가 표시되지 않을 때

시세는 **브라우저에서 외부 API를 직접 호출**하는 구조라, `file://`로 열거나 사내망에서 열면
CORS·방화벽 정책에 막혀 조회되지 않을 수 있습니다. 조회 실패 시에도 맵과 종목 정보는 정상 동작합니다.

1. **`python3 serve.py` 로 실행** → 서버가 대신 호출하므로 대부분 이 단계에서 해결됩니다.
2. 그래도 안 되면 화면 상단 **`시세 진단`** 버튼을 눌러 기록을 확인합니다.
   소스(네이버 → 네이버 일별시세 → Yahoo → Stooq) × 경로(로컬 프록시 → 직접 → 공개 CORS 프록시)별로
   성공/실패와 오류 원인이 모두 남습니다. `복사` 버튼으로 그대로 공유할 수 있습니다.
3. 모든 항목이 실패하면 해당 PC/망에서 `m.stock.naver.com`·`query1.finance.yahoo.com` 접근 자체가
   차단된 상태입니다. `serve.py`의 `ALLOWED_HOSTS`에 사내 시세 엔드포인트를 추가하거나,
   `HTTPS_PROXY`로 사내 프록시를 지정하세요.

| 조회 순서 | 국내 | 해외 |
|---|---|---|
| 1 | 네이버 모바일 `/basic` (현재가·등락률) | Yahoo Finance `chart` |
| 2 | 네이버 `/price` 일별시세 2건으로 등락 계산 | Stooq CSV(전일 종가 기준) |
| 3 | Yahoo Finance `chart` (`005930.KS`) | — |
| 4 | Stooq CSV (`005930.kr`) | — |

### 종목 · 단계 추가

`data/dc-chain.js`(국내) / `data/dc-chain-global.js`(해외)의 배열에 항목을 추가하면 맵에 바로 반영됩니다.
스키마는 `data/dc-chain.js` 상단 주석에 정리해 두었습니다. 한 종목이 여러 단계에 속할 수 있으며(`stages` 배열),
`ysym`은 시세 조회에 쓰이는 Yahoo Finance 심볼입니다. 단계 자체를 늘리려면 같은 파일의 `DC_STAGES`에 추가합니다.
수정 후 단일 파일 버전을 갱신하려면 `datacenter.html`에 두 데이터 파일을 인라인해 `datacenter-standalone.html`로 저장하면 됩니다.

### 종목 선정 기준

각 단계에서 **점유율·수주잔고·고객 기반으로 시장 지위를 확보한 대형주**를 우선 배치했습니다.
국내에서 대형주가 없는 단계(광통신·냉각 등)는 해당 분야 1위 중형주를 `중형`으로 표기해 포함했으며,
맵에서 왼쪽 테두리 색(오렌지=대형 / 회색=중형)과 `대형주만` 필터로 구분할 수 있습니다.

---

## 파일 구조

```
serve.py                     # 로컬 실행 서버 (정적 파일 + 시세 API 프록시 /api/proxy)
index.html                   # 종목 통합 리포트 대시보드 (레이아웃 + CSS + 앱 로직)
standalone.html              # 위 대시보드의 단일 파일 산출물
datacenter.html              # 데이터센터 밸류체인 종목 맵
datacenter-standalone.html   # 위 맵의 단일 파일 산출물
vendor/chart.umd.js          # Chart.js 4.4.1 로컬 번들 (CDN 차단 환경 대비, 실패 시 CDN 폴백)
data/tickers.js              # 종목명↔코드↔시장 매핑 + 조회 함수
data/sample.js               # 샘플/폴백 데이터 생성기 (종목코드 시드 기반 결정적 생성)
data/dc-chain.js             # 밸류체인 9단계 정의 + 국내 종목 데이터(한/영)
data/dc-chain-global.js      # 밸류체인 해외 종목 데이터(한/영)
```

## 종목 추가

`data/tickers.js`의 `TICKERS` 배열에 `{ code, name, market, aliases }` 항목을 추가하면
자동완성·조회에 반영됩니다. 미등록 종목도 6자리 코드로 조회할 수 있습니다.

## 유의사항

본 화면은 정보 제공 목적의 참고 자료이며, 투자 판단과 그 결과에 대한 책임은 이용자에게 있습니다.
샘플 데이터는 실제 수치가 아닌 예시입니다.
밸류체인 맵의 종목 분류는 사업 포트폴리오상 관련성을 기준으로 한 것으로, 기업별 데이터센터 매출 비중은
크게 다릅니다. 규모 구분(대형/중형)은 시장 지위와 시가총액을 함께 고려한 편집 기준이며, 특정 종목의
매수·매도를 권유하지 않습니다.
