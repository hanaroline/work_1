# 미래에셋 세일즈 툴킷 — 실행 방법

## 왜 실행기가 필요한가요?

HTML 파일을 **더블클릭**해서 열면 브라우저 주소가 `file://` 로 시작합니다.
이 방식에서는 브라우저 보안 정책 때문에 **실시간 시세 조회(fetch)가 차단**되어
종목 통합 리포트 대시보드가 **샘플 데이터만** 보여줍니다.
또한 런처(도구 목록)에서 다른 화면을 클릭하면 파일 경로 문제로
`ERR_FILE_NOT_FOUND` 가 뜰 수 있습니다.

아래 실행기는 이 폴더를 **로컬 웹서버(`http://localhost`)** 로 잠깐 띄워 줍니다.
`http://` 주소에서는 위 두 문제가 모두 해결됩니다.
(외부에 공개되지 않으며, 내 PC 안에서만 동작합니다.)

## 실행 방법

### Windows
1. `실행-Windows.bat` 를 **더블클릭**합니다.
2. 검은 창이 뜨고 브라우저가 자동으로 열립니다.
3. 다 쓰면 검은 창에서 `Ctrl + C` 또는 창을 닫으면 종료됩니다.

### macOS
1. `실행-Mac.command` 를 **더블클릭**합니다.
   - "확인되지 않은 개발자" 경고가 나오면: 파일을 **우클릭 → 열기 → 열기** 를 눌러 주세요.
2. 터미널 창이 뜨고 브라우저가 자동으로 열립니다.
3. 다 쓰면 터미널에서 `Ctrl + C` 로 종료합니다.

### 브라우저가 자동으로 안 열릴 때
실행기가 켜져 있는 상태에서 주소창에 직접 입력하세요:

```
http://localhost:8080/tools.html
```

## 준비물

이 실행기는 대부분의 PC에 이미 설치된 **Python** 을 사용합니다.
Python 이 없으면 **Node.js** 를 사용하며, 둘 다 없을 경우 실행기가 설치 링크를 안내합니다.

- Python: https://www.python.org/downloads/ (설치 시 **"Add to PATH"** 체크)
- Node.js: https://nodejs.org/

## 파일 구성

| 파일 | 설명 |
| --- | --- |
| `tools.html` | 4개 도구 런처(도구 목록) — 여기서 시작 |
| `standalone.html` | 종목 통합 리포트 대시보드 (실시간 데이터) |
| `product-compare.html` | 상품 비교기 (ELS·채권·펀드) |
| `pension-tax.html` | 연금·절세 시뮬레이터 |
| `margin-risk.html` | 신용·미수 반대매매 리스크 계산기 |
| `mirae-sales-toolkit.html` | 위 4개를 한 파일에 담은 올인원(더블클릭용, 오프라인) |
| `실행-Windows.bat` / `실행-Mac.command` | 로컬 웹서버 실행기 |
| `serve.mjs` | 실행기가 사용하는 서버 스크립트 |

## 참고: 올인원 파일 (`mirae-sales-toolkit.html`)

인터넷 실시간 데이터가 필요 없고 **더블클릭 한 번**으로 4개 도구를 쓰고 싶을 때는
`mirae-sales-toolkit.html` 하나만 열면 됩니다. (링크 오류 없음)
단, 이 방식은 `file://` 이므로 대시보드는 **샘플 데이터**로 표시됩니다.
실시간 데이터가 필요하면 위 실행기를 사용하세요.
