out/
node_modules/
package-lock.json
package.json

# ELS 수집 실패 시에만 남는 진단자료 — 저장소에 들어갈 이유가 없다
/discovery/
collect-debug/

# 파이썬 바이트코드 — 저장소에 들어갈 이유가 없다
__pycache__/
*.pyc

# 빈칸 검증에서 나오는 임시물 — 쪽 그림과 집계표. 저장소에 남기지 않는다.
# 8/19 판을 만들 때 실제로 2.2MB 가 저장소 뿌리에 쌓였다.
pg/
g.txt
out-beta/
out-prod/

# ETF 페이지 연기 시험이 남기는 화면 갈무리 — 아티팩트로만 올린다
tools/discovery/etf_page.png
