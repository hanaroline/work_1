out/
node_modules/
package-lock.json
package.json

# ELS 수집 실패 시에만 남는 진단자료 — 저장소에 들어갈 이유가 없다
/discovery/
collect-debug/

# 파이썬 바이트코드 — 저장소에 들어갈 이유가 없다
__pycache__/
*.pyc

# 빈칸 검증에서 나오는 임시물 — 쪽 그림과 집계표. 저장소에 남기지 않는다.
# 8/19 판을 만들 때 실제로 2.2MB 가 저장소 뿌리에 쌓였다.
pg/
g.txt
out-beta/
out-prod/

# 투자설명서 공시 원문 — scripts/fetch_prospectus.mjs 가 언제든 다시 받는다.
# 파싱 결과(prospectus_parsed.json)와 앱 입력(data/els-prospectus.js)만 커밋한다.
tools/discovery/prospectus_*.txt
tools/discovery/fund_prospectus_*.txt
collect-debug/
